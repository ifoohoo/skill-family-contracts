{
  "fixtureId": "token-estimate-record.negative.consumption-target-missing",
  "contract": "token-estimate-record",
  "class": "negative",
  "description": "Consumption contract (SG-33) fail-closed guarantee: a record that omits the `tokens` field is rejected, so a consumer can never be handed a record without its consumption target. The consumer-side contract therefore refuses such a record closed (never coerces a missing estimate to 0) instead of displaying or defaulting it.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/token-estimate-record.json",
    "dialect": "2020-12",
    "policy": "strict"
  },
  "expect": { "valid": false, "errorCode": "SFC1001" },
  "data": {
    "schemaVersion": 1,
    "kind": "skill-family.token-estimate-record",
    "estimator": {
      "id": "skill-family.token-estimator",
      "version": "1.0.0"
    },
    "algorithm": "cjk-char-whitespace-split",
    "unit": "tokens",
    "input": {
      "codePoints": 2,
      "inputBytes": 2
    },
    "segmentation": {
      "cjkCharacters": 2,
      "whitespaceSeparators": 0,
      "otherRuns": 0
    },
    "precision": "deterministic-heuristic",
    "scope": "content-text-only",
    "guarantees": [
      "covers-content-text-only",
      "no-model-calls",
      "no-tokenizer-dependency",
      "no-network-access",
      "deterministic-segmentation"
    ]
  }
}
