{
  "fixtureId": "host-operation-receipt.dialect-boundary.unsupported", "contract": "host-operation-receipt", "class": "dialect-boundary",
  "description": "Unsupported schema dialect fails before validation.",
  "validation": { "schemaId": "https://contracts.skill-family.example/v1/host-operation-receipt.json", "dialect": "draft-04", "policy": "strict" },
  "expect": { "valid": false, "errorCode": "SFC1006" },
  "data": { "schemaVersion": 1, "kind": "skill-family.host-operation-receipt" }
}
