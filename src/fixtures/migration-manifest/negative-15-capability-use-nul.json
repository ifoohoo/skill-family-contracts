{
  "fixtureId": "migration-manifest.negative.capability-use-nul",
  "contract": "migration-manifest",
  "class": "negative",
  "description": "A capability use retained guarantee cannot contain a NUL character.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/migration-manifest.json",
    "dialect": "2020-12",
    "policy": "strict"
  },
  "expect": { "valid": false, "errorCode": "SFC1001" },
  "data": {
    "schemaVersion": 1,
    "kind": "skill-family.migration-manifest",
    "capabilityUseContext": { "scope": "harness", "locale": "en" },
    "capabilityUses": [
      {
        "useId": "safe-output-write",
        "problemStatement": "Write output without leaving a partial file",
        "constraints": [],
        "retainedGuarantees": ["The consumer still\u0000chooses the output artifact"]
      }
    ]
  }
}
