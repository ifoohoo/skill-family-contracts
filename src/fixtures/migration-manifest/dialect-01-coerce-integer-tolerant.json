{
  "fixtureId": "migration-manifest.dialect-boundary.coerce-integer-tolerant",
  "contract": "migration-manifest",
  "class": "dialect-boundary",
  "description": "Coercion policy boundary: a string manifestVersion passes only because the tolerant policy enables Ajv integer coercion.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/migration-manifest.json",
    "dialect": "2020-12",
    "policy": "tolerant"
  },
  "expect": { "valid": true },
  "data": {
    "schemaVersion": 1,
    "kind": "skill-family.migration-manifest",
    "manifestVersion": "1"
  }
}
