{
  "fixtureId": "adapter-build-manifest.dialect-boundary.unsupported", "contract": "adapter-build-manifest", "class": "dialect-boundary",
  "description": "Unsupported schema dialect fails before validation.",
  "validation": { "schemaId": "https://contracts.skill-family.example/v1/adapter-build-manifest.json", "dialect": "draft-04", "policy": "strict" },
  "expect": { "valid": false, "errorCode": "SFC1006" },
  "data": { "schemaVersion": 1, "kind": "skill-family.adapter-build-manifest" }
}
