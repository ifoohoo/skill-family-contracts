{
  "fixtureId": "public-boundary-declaration.positive.valid",
  "contract": "public-boundary-declaration",
  "class": "positive",
  "description": "A complete public boundary declaration: one public root, explicit public files, one required release path and one forbidden internal path.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/public-boundary-declaration.json",
    "dialect": "2020-12",
    "policy": "strict"
  },
  "expect": { "valid": true },
  "data": {
    "schemaVersion": 1,
    "kind": "skill-family.public-boundary-declaration",
    "declarationId": "public-site",
    "declaredVersion": "0.6.0",
    "publicRoots": ["docs/public"],
    "requiredPaths": ["docs/public/index.html"],
    "forbiddenPaths": ["docs/internal"],
    "publicFiles": ["docs/public/index.html", "docs/public/site-baseline.json"],
    "note": "Public site boundary for the 0.6.0 candidate release."
  }
}
