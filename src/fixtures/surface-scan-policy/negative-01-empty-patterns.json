{
  "fixtureId": "surface-scan-policy.negative.empty-patterns",
  "contract": "surface-scan-policy",
  "class": "negative",
  "description": "A policy with no forbidden patterns at all is rejected: a scan that can never fail closed is not a policy.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/surface-scan-policy.json",
    "dialect": "2020-12",
    "policy": "strict"
  },
  "expect": { "valid": false, "errorCode": "SFC1001" },
  "data": {
    "schemaVersion": 1,
    "kind": "skill-family.surface-scan-policy",
    "pathPatterns": [],
    "contentPatterns": [],
    "allowedUses": ["fictitious-project-inspection"]
  }
}
