{
  "fixtureId": "structured-scan-policy.dialect-boundary.unsupported",
  "contract": "structured-scan-policy",
  "class": "dialect-boundary",
  "description": "A structured scan policy document written in draft-04 dialect; the contracts pipeline only accepts the registered 2020-12 dialect for this object.",
  "validation": {
    "schemaId": "https://contracts.skill-family.example/v1/structured-scan-policy.json",
    "dialect": "draft-04",
    "policy": "strict"
  },
  "expect": { "valid": false, "errorCode": "SFC1006" },
  "data": {
    "$schema": "http://json-schema.org/draft-04/schema#",
    "schemaVersion": 1,
    "kind": "skill-family.structured-scan-policy",
    "allowedNetworks": ["192.0.2.0/24"],
    "approvedRegistries": ["https://registry.example.test/"],
    "approvedCoordinates": ["fictitious-scope/fictitious-lockfile-parser@1.2.3"],
    "formatAdapters": [{ "name": "fictitious-lockfile", "pathPattern": "pnpm-lock\\.yaml$" }],
    "symlinkPolicy": { "mode": "forbid" },
    "binaryPolicy": { "mode": "fail-closed", "approvedPaths": [] }
  }
}
